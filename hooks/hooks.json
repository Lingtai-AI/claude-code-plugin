{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -d .lingtai/human/mailbox/inbox ]; then count=$(find .lingtai/human/mailbox/inbox -name message.json 2>/dev/null | wc -l | tr -d ' '); echo \"LingTai network detected with $count message(s) in inbox.\"; fi",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
